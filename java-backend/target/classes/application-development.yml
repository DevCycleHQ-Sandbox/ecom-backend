spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  
logging:
  level:
    root: INFO
    com.shopper: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG